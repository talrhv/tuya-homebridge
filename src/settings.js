"use strict";

/**
 * Keep these in sync with:
 * - package.json "name"
 * - config.schema.json "pluginAlias" / platform name
 */

const PLATFORM_NAME = "TuyaPlatform";
const PLUGIN_NAME = "homebridge-tuya-platform-talrhvfork-test";

module.exports = {
  PLATFORM_NAME,
  PLUGIN_NAME,
};
